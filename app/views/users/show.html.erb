<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
<% @user.projects.each do |project| %>
<%= project.name %>

<% end %>

<hr>

<p> Proyectos :</p>

<% @projects.each do |project| %>

<%= project.name %>
<%= link_to 'Asignar', allocations_path(project_id: project.id, user_id: @user.id), method: :post %>
<% end %>

</p>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
